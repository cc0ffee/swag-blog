---
import '@/styles/global.css'
import '@/styles/typography.css'

import Head from '@/components/head.astro'
import HomeSidebar from '@/components/home-sidebar.astro'
import PostTopbar from '@/components/post-topbar.astro'
import { SITE } from '@/consts'

interface Props {
  title: string
}

const { title } = Astro.props
---

<!doctype html>
<html
  class="bg-white text-[var(--home-brown)] scheme-light"
  lang={SITE.locale}
  data-sidebar-collapsed="true"
>
  <Head>
    <slot name="head" />
  </Head>
  <body class="font-sans antialiased">
    <HomeSidebar />
    <PostTopbar title={title} />
    <div
      class="post-main min-h-screen transition-[margin-left] duration-300 ease-in-out"
      style="margin-left: var(--home-sidebar-width); padding-top: var(--home-topbar-height);"
    >
      <slot />
    </div>
  </body>
</html>
